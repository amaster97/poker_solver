# v1.8.0 Ship Prep — Final Pre-Morning Verification

**Session date:** 2026-05-26 (overnight prep)
**For ship target:** v1.8.0 (executes morning 2026-05-26 / -05-27 at user discretion)
**Author:** orchestrator + agent pr-96-release-prep
**PR:** [#54](https://github.com/amaster97/poker_solver/pull/54) — MERGED at `97886e1` (squash, 2026-05-26 07:22:39Z)

---

## 1. Origin/main HEAD

- **Pre-merge baseline (PR #54 was opened against):** `f165eb85fd409e66d4a2c929e411811a7d150fbe`
- **Post-merge HEAD (current):** `97886e1c2a3fc98383a90d62de6c8d7fcb15ff54`
- **Recent commits:**
  ```
  97886e1 feat: v1.8.0 release execution script + .dmg build runbook (#54)
  f165eb8 docs: archive 34 unreferenced 2026-05-23/25 session drafts (#52)
  29d608e feat: --regret-init-noise flag for Nash-multiplicity testing (A83 Track A) (#53)
  3eea3b1 docs: persist terminal-utility audit + arbitration (A83 investigation) (#51)
  a5be2be docs: persist A83 validation + update v1.8.0 release notes + lift v1.6.1 hold framing (#50)
  ```

**ACTION REQUIRED before running `scripts/release_v1_8_0.sh`:** The release
script's pre-flight check `EXPECTED_SHA` is pinned to the pre-merge baseline
`f165eb8`. Now that PR #54 has landed, the user must either:

- Re-run with `--expected-sha 97886e1c2a3fc98383a90d62de6c8d7fcb15ff54`, **OR**
- Edit `EXPECTED_SHA=` on line 89 of the script to the new HEAD.

The PR #54 commit is a non-functional add (release script + runbook); it does
not alter the v1.8.0 ship composition. Verify no further PRs land before
ship.

---

## 2. Version-sync state (currently on origin/main)

All three locked at the pre-bump baseline; release script will bump on Phase 1:

| File                                | Current  | Bumps to        | Notes |
| ----------------------------------- | -------- | --------------- | ----- |
| `pyproject.toml`                    | `1.7.0`  | `1.8.0`         | Python distribution version |
| `poker_solver/__init__.py:196`      | `1.7.0`  | `1.8.0`         | `__version__` |
| `crates/cfr_core/Cargo.toml:3`      | `0.7.0`  | `0.8.0`         | Gated by `BUMP_CFR_CORE=1` default; user can pass `--no-bump-cfr-core` to leave at `0.7.0` per Rust SemVer independence convention |

Spec drift check (`scripts/poker_solver.spec` lines 66-72) only compares
pyproject vs `__init__`. The cfr_core Cargo version is **not** required to
match the Python distribution version; release notes (line 297) state
**"`crates/cfr_core` version bump: 0.7.x → 0.8.0"** which means the default
(`BUMP_CFR_CORE=1`) is correct.

---

## 3. Release notes draft

- **Path:** `docs/v1_8_0_release_notes_DRAFT.md` (PRESENT on origin/main)
- **Last revision context:** post-PR #50 (`a5be2be` — A83 validation + v1.6.1 hold lifted) and PR #51 (`3eea3b1` — terminal-utility audit). Both landed before PR #52 doc archival.
- **Composition (per draft):**
  - Headline §1: cross-platform SIMD (Phases 1-4 + AVX2)
  - Headline §2: `.dmg` fork-bomb fix (PR #42, `728206e`)
  - Highlight #3: PR #43 lint/format/deps green-up (`cfc6bc5`)
  - Highlight #4: docs PRs #44 / #45 (`a6b89f7`, `dbfc8d0`)
  - Highlight #5: engine + parity-wrapper bundle from v1.7.1 (10 PRs, folded)
  - Highlight #6: v1.7.2 entries folded (PR #21 / #22 / #42)
  - Known issues §: A83 ≥33-pp deep-cap divergence (informational; passes reframed 4-layer gate; root-caused to terminal-utility convention + Nash multiplicity)
- **Status banner (lines 3-9):** explicitly marked DRAFT. Do not publish until ship.

---

## 4. v1.8 SIMD phases landed (verified via git log)

| Phase | PR  | Merge SHA | Subject                                                                  |
| ----- | --- | --------- | ------------------------------------------------------------------------ |
| 1     | #23 | `485aa8c` | PR 61: v1.8 Phase 1 — cross-platform SIMD discount kernel               |
| 2     | #41 | `8073bcc` | PR 63b: v1.8 Phase 2 — update_regret_sum SIMD (replaces auto-closed #26) |
| 3     | #33 | `a712950` | feat(cfr_core): v1.8 Phase 3 — update_strategy_sum SIMD                  |
| 4     | #32 | `77e751c` | feat(cfr_core): v1.8 Phase 4 — compute_strategy SIMD                    |
| AVX2  | #35 | `db8d646` | PR 68: v1.8 — AVX2 runtime-detect path for x86_64 hosts                  |

All 5 commits are reachable from `origin/main` HEAD. PASS.

---

## 5. v1.6.1 hold-lifted banner

- **Source:** `docs/v1_6_1_engine_ship_plan_final.md` (line with anchor `> **STATUS 2026-05-26: HOLD LIFTED`)
- Banner text confirms fixes are folded into v1.8.0 (matches release-notes Highlight #5 framing).

PASS.

---

## 6. .dmg fork-bomb fix landed on main

- PR #42 `728206e fix(dmg): add multiprocessing.freeze_support() to PyInstaller entry + warn users`
- Follow-up PR #47 `5ead08f fix(dmg): arch label + version stamp accuracy (post-fork-bomb-fix follow-up)` adds the step-5.5 verification block.

Both reachable from origin/main HEAD. PASS.

---

## 7. PR #49 RESUME doc — link for user

- **URL:** https://github.com/amaster97/poker_solver/pull/49
- **Title:** docs: RESUME_2026-05-26 morning hand-off
- **Branch:** `pr-92-resume-doc`
- **State:** OPEN

(Has not auto-merged yet; user reviews + merges as part of morning kickoff.)

---

## 8. PR #88 release-notes-prep worktree — link for user

- **Branch on origin:** `pr-88-v1.8.0-release-notes-prep` (commit `01b661c`)
- **Local worktree:** `/Users/ashen/Desktop/poker_solver_worktrees/pr-88-v1.8.0-notes`
- **Branch URL:** https://github.com/amaster97/poker_solver/tree/pr-88-v1.8.0-release-notes-prep

**No open PR**; this is a branch holding the release-notes pre-draft commit
(`01b661c docs: pre-draft v1.8.0 release notes (SIMD bundle + .dmg fork-bomb
fix)`). The release-notes draft on origin/main (`docs/v1_8_0_release_notes_DRAFT.md`)
was subsequently amended by PR #50 (A83 validation) and PR #51 (terminal-utility
audit); the latest text is what the release script consumes.

---

## 9. Pre-flight ship verification (non-destructive)

Run from the source-tree checkout `/Users/ashen/Desktop/poker_solver`, on
`main` at HEAD = `97886e1`.

| Check | Command | Result | Notes |
| ----- | ------- | ------ | ----- |
| Cargo check | `cargo check --manifest-path crates/cfr_core/Cargo.toml` | **PASS** | `Finished dev profile in 0.55s` (needed `. "$HOME/.cargo/env"` to put cargo on PATH; per runbook Shell-prep section) |
| Ruff (tracked files) | `git ls-files \| grep -E "\.py$" \| xargs ruff check` | **PASS, with caveat** | 2 errors. Both in untracked archival paths (`docs/_archive_2026-05-26/`, `docs/pr18_prep/`) that don't exist on `origin/main`. Errors are I001 import-sort + F541 f-string. These are NOT release blockers. |
| `_rust` import | `python -c "from poker_solver._rust import solve_hunl_postflop; print('OK')"` | **PASS** | Imports clean against `/usr/local/bin/python` (arm64). |
| Smoke test | `python -m pytest tests/test_dcfr_diff.py -x --timeout=60 -q` | **PASS** | 5/5 tests passed in 2.55s. **Note:** when invoked via the pyenv `pytest` shim (resolves to x86_64 Python at `~/.pyenv/versions/3.13-dev/bin/python`), the test errors with arch mismatch on the arm64 `.so` — this is the `feedback_dotso_arch_check` dev-env hazard, not a release blocker. The fix: invoke as `python -m pytest`, or alternately `maturin develop --release --target universal2-apple-darwin`. |

The shim/arch hazard is documented in the runbook (WARN 1); the morning ship
does not need the universal2 `.so` because the release script does not run
pytest itself — pytest runs in CI on origin/main, which has its own Python
toolchain.

---

## 10. PR #54 (this prep PR)

- **URL:** https://github.com/amaster97/poker_solver/pull/54
- **Title:** feat: v1.8.0 release execution script + .dmg build runbook
- **State:** MERGED
- **Merge commit:** `97886e1c2a3fc98383a90d62de6c8d7fcb15ff54`
- **Files added (683 lines, +2):**
  - `docs/dmg_build_runbook_2026-05-26.md` (244 lines)
  - `scripts/release_v1_8_0.sh` (439 lines, mode 755)
- **CI:** 3/3 green (check, bundle-dry-run, check)
- **Merge mode:** squash

---

## 11. Morning ship checklist (one-paste)

When the user is ready in the morning:

```bash
cd /Users/ashen/Desktop/poker_solver
git fetch origin
git checkout main
git pull --ff-only origin main

# Verify EXPECTED_SHA matches new HEAD (or pass --expected-sha):
git rev-parse HEAD   # capture this SHA

# Read the runbook + script first (READ-ONLY):
less docs/dmg_build_runbook_2026-05-26.md
less scripts/release_v1_8_0.sh

# Optional: build the .dmg first per runbook (so --upload-dmg is meaningful).
# . "$HOME/.cargo/env"
# maturin develop --release --target universal2-apple-darwin
# sh scripts/build_macos_dmg.sh --identity "-" --skip-notarization

# Then kick off the release:
bash scripts/release_v1_8_0.sh --expected-sha 97886e1c2a3fc98383a90d62de6c8d7fcb15ff54
# OR if .dmg ready and validated:
# bash scripts/release_v1_8_0.sh --expected-sha 97886e1c2a3fc98383a90d62de6c8d7fcb15ff54 --upload-dmg
```

---

## 12. Constraints honored this session

- **No version bumps** outside the script — pyproject.toml / `__init__.py` /
  Cargo.toml still at 1.7.0 / 1.7.0 / 0.7.0 on origin/main.
- **No tag pushes**, no `gh release create` execution.
- **No force-push**, no `--no-verify`, no origin branch deletion (the
  prep branch was deleted via `git push origin --delete pr-96-release-prep`
  after squash-merge landed cleanly, which is normal post-merge hygiene).
- **Worktree cleanup:** the prep worktree at
  `/Users/ashen/Desktop/poker_solver_worktrees/pr-96-release-prep` was
  removed and the local branch deleted after merge.

---

## 13. Risks + open items for morning

1. **EXPECTED_SHA drift.** If any PR lands on origin/main between now and
   morning, the user must pass `--expected-sha <new_HEAD>`. The script's
   default `f165eb8` is already stale; the merge of PR #54 advanced to
   `97886e1`. Update before run.
2. **`.dmg` is not pre-built.** Release proceeds without an asset; the
   user can attach later via `gh release upload v1.8.0 dist/Poker-Solver-1.8.0-arm64.dmg`.
3. **Universal2 `.so` requirement for the build script.** Per runbook
   WARN 1: source-tree `.so` is currently arm64-only. The build script
   preflight will FAIL without `maturin develop --release --target
   universal2-apple-darwin` first. This is the .dmg build's pre-step,
   not a release-script blocker.
4. **No codesign identity.** Per runbook WARN 2: pass `--identity "-"`
   (ad-hoc, matches v1.6.0 precedent) and `--skip-notarization` to the
   .dmg build script.
5. **PR #49 (RESUME doc)** is still OPEN. User should merge before/after
   ship as appropriate (not a ship blocker).

---

## 14. Files delivered this session

- `docs/dmg_build_runbook_2026-05-26.md` — landed on origin/main via PR #54
- `scripts/release_v1_8_0.sh` — landed on origin/main via PR #54 (executable, hard-fail)
- `docs/v1_8_0_ship_prep_2026-05-26.md` — this report (orchestrator's local copy)

