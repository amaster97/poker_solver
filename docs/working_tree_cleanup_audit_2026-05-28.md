# Working-tree cleanup audit — 2026-05-28

**Scope:** post-burst (v1.8.2 ship) audit of `/Users/ashen/Desktop/poker_solver` working tree.
**Mode:** triage every tracked-modified and untracked path; classify SHIP / KEEP-PRIVATE / IGNORE.
**Authored from worktree:** `/Users/ashen/Desktop/poker_solver_worktrees/cleanup-working-tree-2026-05-28`.

---

## TL;DR

| Bucket | Count | Action |
|---|---|---|
| Tracked-modified files | 1 (`Cargo.lock`) | **FIX in this PR** — origin/main has stale lockfile (0.8.0) vs Cargo.toml (0.8.2) |
| Untracked, substantive Rust work | 1 (`crates/cfr_core/examples/rvr_bench.rs`) | **SHIP in this PR** — v1.8 SIMD perf bench, compile-clean, no PR existed |
| Untracked, substantive docs | 25+ in `docs/*.md` and `docs/*_prep/` | **KEEP UNTRACKED on origin** — already tracked on `backup/integration` per Option C dual-channel; private-only artifacts |
| Untracked, dead scripts | 1 (`scripts/ship_v1_6_1_engine.sh`) | **IGNORE** — superseded by v1.8.2 ship; never tracked; safe to delete locally |
| Untracked, private-only scripts | 1 (`scripts/cleanup_pr_branches.sh`) | **IGNORE on origin** — tracked on `backup/integration` |
| Pre-ship stash | `stash@{1}` PRE-V1.8.2-SHIP-stash | **REDUNDANT** — content matches PR #139 commit `0c89e73`; safe to drop; flagged for user, NOT auto-dropped |
| Suspicious finding | `exploit.rs` clean on HEAD | None — verified PR #139 work landed in branch `perf/br-walk-terminal-cache-task50` (PR still OPEN) |

---

## 1. Tracked modifications

### `Cargo.lock` — version drift (origin/main bug)

| Side | `cfr_core` version |
|---|---|
| `Cargo.toml` on origin/main | `0.8.2` (bumped by PR #134 commit `16c92e6`) |
| `Cargo.lock` on origin/main | `0.8.0` (NOT regenerated by PR #134) |
| `Cargo.lock` in user's working tree | `0.8.2` (correct) |

**Root cause:** PR #134 ("chore: bump version to 1.8.2 for release") modified `crates/cfr_core/Cargo.toml`, `poker_solver/__init__.py`, and `pyproject.toml` — but did **not** regenerate `Cargo.lock`. The local working tree happens to have the corrected lockfile (regenerated when an agent ran cargo locally post-bump).

**Action:** ship the corrected `Cargo.lock` in this cleanup PR. Reproduction:
```bash
cargo update -p cfr_core --offline
# Cargo.lock 0.8.0 -> 0.8.2 (1 line changed)
```

**Severity:** LOW (does not break builds; `cargo build` regenerates the lock; CI tolerates this). But it's a real audit-trail discrepancy and trivially fixed.

---

## 2. Untracked Rust work — SHIP

### `crates/cfr_core/examples/rvr_bench.rs`

**Substantive content:** 113-line Cargo example, wall-clock perf bench for `dcfr_vector::solve_range_vs_range_postflop`. Comment block reads:

> v1.8 SIMD perf bench: vector-form DCFR (`dcfr_vector.rs`) wall-clock.
> [...] This bench is the workload the v1.8 plan claims to accelerate — the per-(hand, action) hot loops in `dcfr_vector::VectorDCFR::solve`. The existing `dcfr_bench.rs` only exercises `dcfr.rs` (scalar info-state DCFR for Kuhn/Leduc), which was already SIMD-wired at v1.0.1 / PR 8; it cannot validate the v1.8 incremental.

**Sanity checks:**

| Check | Result |
|---|---|
| `cargo build --release --manifest-path crates/cfr_core/Cargo.toml --example rvr_bench` | PASS (compile-clean in 7.5s) |
| Was it ever tracked on any branch? | NO (verified `git log --all --diff-filter=A -- crates/cfr_core/examples/rvr_bench.rs` → empty) |
| Was it in any stash? | NO (verified all 9 stashes; only stash@{1} touches `dcfr_vector`/`exploit`/`rvr_profile`, not `examples/rvr_bench`) |
| Companion files in `examples/` already tracked? | YES — `examples/build_preflop_equity.rs` is on origin/main; this dir is a real Cargo `[[example]]` directory |

**Action:** add to this PR. The file describes its workload, runs, and emits structured JSON — usable as-is for the v1.8 perf claim verification thread.

---

## 3. Untracked docs — KEEP UNTRACKED on origin

Per the Option C dual-channel rule (memory: `feedback_public_repo_hygiene.md`), audit/internal docs **must not be pushed to public origin/main**. They live on `backup/integration` (private mirror). Spot-verified:

```bash
$ git ls-tree -r backup/integration -- docs/persona_test_results/ | wc -l
# >5 files present on backup
$ git ls-tree -r backup/integration -- docs/pr8_prep/ | wc -l
# >5 files present on backup
$ git ls-tree -r origin/main -- docs/persona_test_results/ docs/pr8_prep/ | wc -l
# 0 files (never on public)
```

These 25+ untracked docs are **expected debris on a working tree that participates in dual-channel sync**. They are not orphan agent-debris; they are real artifacts that an aggregator agent will eventually merge to `backup/integration`. Adding them to this public-channel PR would violate the Option C filter.

**Notable untracked docs (high-value, all stay on private):**

- `docs/post_burst_audit_2026-05-27.md` (156 lines) — v1.8.0 stability check; flags 3 dangling release-notes refs as `v1.8.1` candidate
- `docs/master_plan_remaining_2026-05-27.md` (129 lines) — PLAN.md vs origin/main reconciliation
- `docs/v1_8_1_candidate_doc_overclaims.md` — HIGH-severity finding: published v1.8.0 release notes reference 3 docs not in the tag + 5x "PR #93" naming collision
- `docs/plan_md_sync_2026-05-27.md` (129 lines) — plan-sync ledger
- `docs/persona_test_results/` (34 files) — full persona retest history W1.1..W3.5

**Action:** none in this PR. Flagged for next aggregator/backup-mirror sync.

---

## 4. Untracked scripts

### `scripts/cleanup_pr_branches.sh` (296 lines)

Already tracked on backup at commits `3475ca9` and `a18917a`. Private-only utility for cleaning STATUS.md leaks from old PR branches. **Stay untracked on origin.**

### `scripts/ship_v1_6_1_engine.sh` (349 lines)

**Dead code.** Was a Path-D ship script for v1.6.1. Never tracked anywhere (`git log --all --oneline -- scripts/ship_v1_6_1_engine.sh` → empty). v1.8.2 has long since shipped; this script is obsolete. **Safe to delete locally.** Not deleted by this PR (out of scope), but flagged.

---

## 5. Stash hygiene

### `stash@{1}` PRE-V1.8.2-SHIP-stash (rvr_profile + dcfr_vector + exploit)

```
crates/cfr_core/benches/rvr_profile.rs |   1 +
crates/cfr_core/src/dcfr_vector.rs     |   1 +
crates/cfr_core/src/exploit.rs         | 507 ++++++++++++++++++++++++++++++---
3 files changed, 475 insertions(+), 34 deletions(-)
```

**Comparison with PR #139** (branch `perf/br-walk-terminal-cache-task50`, commit `0c89e73`):

```
crates/cfr_core/benches/rvr_profile.rs    | 1 +
crates/cfr_core/src/dcfr_vector.rs        | 1 +
crates/cfr_core/src/exploit.rs            | 461 +++--
3 files changed
```

Diff between stash and `0c89e73` is **whitespace and comment-wrapping only** (~3 lines of doc-comment re-wrap in `exploit.rs`).

**Verdict:** stash@{1} is **redundant** — its content was preserved as PR #139 (currently OPEN, awaiting merge). Per memory rule (`feedback_branch_ops.md` — "never `git stash drop` post-conflict"), this stash is NOT auto-dropped. Flagged for user to `git stash drop stash@{1}` once PR #139 merges.

### Other stashes (8 total)

| ID | Branch | Content | Disposition |
|---|---|---|---|
| stash@{0} | feat/cli-default-true-nash | wip pre-rebase, `cli.py` 171 lines | Likely covered by PR #136 (merged); user to verify |
| stash@{2} | feat/pushfold-return-ev-task48 | WIP push-fold | Likely covered by PR #125 (merged); user to verify |
| stash@{3} | feat-cli-subgame | WIP CLI subgame | Likely covered by PR #127 (merged); user to verify |
| stash@{4} | docs/readme-quickstart-fix | PR #98 PASS confirm | Verify against PR #104 (merged) |
| stash@{5} | proposal/dcfr-alpha-guard | DCFR alpha proposal | Verify against PR #113 (merged) |
| stash@{6} | pr-75-purge-rust-convention | convention purge recovery | Verify against PR #78 (merged) |
| stash@{7} | (no branch) | README v1.5.1→v1.6.0 bump | Likely stale; verify |
| stash@{8} | main | pre-comprehensive-review backup | 2311-line diff; large blast radius, KEEP for now |

**Action:** flagged for separate stash-prune session; not part of this PR.

---

## 6. Suspicious findings

### `crates/cfr_core/src/exploit.rs` — clean on HEAD (as expected)

User task brief said: *"`crates/cfr_core/src/exploit.rs` — was committed to PR #139 via my branch; should be clean now. If still modified, that's stale."*

Verified: `git status crates/cfr_core/src/exploit.rs` → clean. PR #139 is OPEN (not merged) but the work lives on branch `perf/br-walk-terminal-cache-task50`. The user's local `main`-tracking working tree shows `exploit.rs` at the unchanged origin/main blob.

**No suspicious commit-not-PR'd finding here.** All substantive engine work is on a branch + PR. The only unpushed artifact was the perf bench file (item 2 above), which this PR resolves.

### Main worktree is on `docs/b10-per-combo-plan`, not `main`

The user's main repo directory (`/Users/ashen/Desktop/poker_solver`) is currently checked out on branch `docs/b10-per-combo-plan` at commit `2261fc7` — not on `main`. The Cargo.lock and rvr_bench artifacts are the difference between that branch's tree and `origin/main`. This is consistent with active multi-branch worktree development; no anomaly.

---

## 7. Action summary

| Item | Action | Where |
|---|---|---|
| `Cargo.lock` 0.8.0 → 0.8.2 | Ship | This PR |
| `crates/cfr_core/examples/rvr_bench.rs` | Ship | This PR |
| 25+ untracked docs in `docs/*` | Leave untracked on origin | Will be picked up by next `backup/integration` sync |
| `scripts/cleanup_pr_branches.sh` | Leave untracked on origin | Already on `backup/integration` |
| `scripts/ship_v1_6_1_engine.sh` | Flag for local delete | User decision |
| `stash@{1}` PRE-V1.8.2 | Flag drop after PR #139 merges | User decision |
| Other 8 stashes | Flag for separate prune session | User decision |
| Main repo on `docs/b10-per-combo-plan` branch | Note only; no action | Expected state |
