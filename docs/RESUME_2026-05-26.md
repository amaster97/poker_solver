# RESUME — 2026-05-26 morning

> **STATUS 2026-05-27 ~09:30 UTC (eighth-pass refresh — convention purge LANDED, ship-ready):**
> The convention purge (PR #78 / `37e5be1`) — the keystone fix that was IN FLIGHT at last refresh — **MERGED to main**. The "rust" terminal-utility convention has been **empirically purged**: there is no `TerminalUtilityConvention` enum, no `terminal_utility_convention` field on `HUNLConfig`, no per-leaf convention branching. One canonical Brown formula is the only utility. v1.5 Brown apples-to-apples **PASSES under the gated SANITY layer on both A83 + K72 fixtures** post-purge. Persona W4.3 (Priya Brown apples-to-apples parity) reclassified from "PASS-aggregator" → **"PASS-strict"** (PR #83). v1.6.1 hold-lift **VALIDATED** (PR #84) — the prior hold-lift conclusion is now empirically confirmed. v1.8.0 ship trigger wrapper (PR #80, `scripts/release_v1_8_0_trigger.sh`) is VERIFIED via `--dry-run`. **v1.8.0 ship is ready to fire as soon as the final TBD-substitution PR lands** (replaces the 2 remaining validator placeholders in `docs/v1_8_0_release_notes_DRAFT.md`: `<TBD-POST-PURGE-RESIDUAL>` + `<TBD-POST-PURGE-MAX-Δ-A83-2K>`).

**Headline: A83 33pp gap is FULLY RESOLVED with a nuanced story** (not a single-cause closure):
1. **The convention purge (PR #78, `37e5be1`) eliminated the regret-update bias** that the PR #93 terminal-utility ablation surfaced (max |Δ| 12-50pp under varied-convention / fixed-init-regret). The reach-weighted offset bias is structurally impossible going forward.
2. **The residual deep-cap divergence is genuine Nash multiplicity at indifference manifolds** (PR #68 empirical confirmation: `max |Δ| = 0.998499` under fixed-convention / `ε=1e-9` init-regret perturbation). This is NOT a bug; deep-cap HUNL has indifference manifolds where multiple Nash equilibria are valid.
3. **Per-cell strict max |Δ| post-purge** (v1.5 Brown apples-to-apples @ 2000 iters): **K72 0.852** (was 0.868 pre-purge — tightened by ~2%), **A83 0.907** (was 0.907 pre-purge — unchanged at four-digit precision). The purge tightened K72 slightly but did NOT collapse the residual; the multiplicity floor is the binding constraint.
4. **Reframed 4-layer gate PASSES** on both fixtures (top-action ≥95%, 100% action-count match, 0 shallow violations, gated SANITY layer green). Strict per-cell layer remains informational, consistent with `feedback_nash_multiplicity_acceptance.md`.
5. **EV-of-action invariance is the load-bearing testable invariant** for the residual multiplicity claim (per `docs/ev_invariance_sanity_gauntlet_design_2026-05-27.md`); strategy-probability convergence is non-falsifiable in the multiplicity zone.

**Ship-blocker hot-patch (2026-05-26 ~04:22 UTC):** `solve_hunl_postflop` scalar Rust + `solve --hunl-mode postflop --backend rust` CLI hard-fail on missing `initial_hole_cards` instead of silently returning empty-strategy. PR #69 (`98fb503`). Closes the v1.8 ship-blocker that caused the INVALIDATED Gate 4 200K v2 + A83 Track A v1 nohup runs.

**Session window:** 2026-05-26 ~01:30 UTC → 2026-05-27 ~09:30 UTC (autonomous, Stage-3 trust-the-draft + per-PR feature-branch discipline; multi-AFK refresh).
**Origin/main HEAD:** `a44c93e` — `docs: v1.6.1 hold-lift VALIDATED post-purge (W4.3 strict-PASS confirms) (#84)`
**Backup/main HEAD:** `5ead08f` (last sync; behind by 38+ commits — re-sync queued)
**Backup/integration HEAD:** `daab5f5` (origin/main folded in via merge commit at PR #47 boundary; PLAN.md + autonomous_burst_release_plan.md preserved as integration-only)

> **Update log:** Drafted at ~07:00 UTC 2026-05-26 (PR #49). Second burst (~07:00→07:45 UTC) landed PRs #50-#60. Third pass (~07:55 UTC): end-of-session metrics. Fourth pass (~04:30 UTC, post-probe): folded in A83 Nash multiplicity empirical confirmation (PR #68) + ship-blocker hot-patch (PR #69) + PRs #61-#67. **Fifth pass (2026-05-26 ~09:20 UTC):** A83 status downgraded from "FULLY SETTLED" to "IN FLUX" after PR-93 ablation contradicted the Nash-multiplicity-only framing. **Sixth pass (2026-05-27 ~03:57 UTC):** VICTORY banner replaced after `pr-75-purge-rust-convention` analysis pinned the "rust" convention as a real bug requiring purge. **Seventh pass (2026-05-27 ~08:00 UTC):** mid-AFK refresh — fold in fourth-burst PRs #70-#76 + convention-purge recovery story. **Eighth pass (THIS UPDATE, 2026-05-27 ~09:30 UTC):** **convention purge LANDED (PR #78)**; fold in fifth-burst PRs #77-#84 (KEYSTONE + supporting docs); A83 status: FULLY RESOLVED via nuanced two-mechanism story (convention purge closed the regret-update bias + Nash multiplicity residual remains but is acceptable + handled by EV invariance gauntlet); W4.3 PASS-aggregator → PASS-strict; v1.6.1 hold-lift VALIDATED; v1.8.0 ship trigger wrapper VERIFIED via dry-run; ship ready to fire on final TBD-substitution PR landing.

---

## TWO MAJOR FINDINGS — STATUS UPDATE

### 1. Terminal-utility convention + A83 Nash multiplicity — **FULLY RESOLVED** (post-PR-78 convention purge)

**STATUS (CORRECTED 2026-05-27 ~09:30 UTC, post-PR-#78):** Finding #1 is **FULLY RESOLVED via a nuanced two-mechanism story**: the convention purge (PR #78, `37e5be1`) closed the regret-update bias surfaced by PR #93 ablation; the residual deep-cap divergence is the genuine Nash multiplicity surfaced by PR #68. Both halves are now empirically backed + persistently on main.

**The convention purge (PR #78, `37e5be1`) — KEYSTONE:**
- Removed the "rust" terminal-utility convention entirely. No `TerminalUtilityConvention` enum, no `terminal_utility_convention` field on `HUNLConfig`, no `brown_base_pot` helper, no per-leaf convention branching.
- One canonical Brown formula is the only utility:
  ```
  pot_total = initial_pot + (c0 - init_c0) + (c1 - init_c1)
  winner_u  = pot_total - cs_winner    (in BB)
  loser_u   = -cs_loser                (in BB)
  tie_each  = pot_total/2 - cs_player  (in BB)
  ```
- Game is now **constant-sum** with `u[0] + u[1] = initial_pot / big_blind` per leaf (not zero-sum when dead money is present). DCFR convergence proofs only need bounded utilities + finite action set, so this is fine.
- Numeric example (P0 folds at default river fixture, BB=100, initial_pot=1000, initial_contributions=(500,500)):
  - rust (old, wrong): `u = (-5, +5)` — only loser's subgame contrib changes hands
  - canonical (new):   `u = ( 0, +10)` — winner collects full pot including dead money
- 8 files touched (engine: `hunl.rs`, `exploit.rs`, `hunl.py`; tests: 5 files rebaselined including pre-existing broken asserts in `test_03` + `test_06` from PR 35 Fix C drift).
- **Retracts:** `docs/terminal_utility_arbitration_2026-05-26.md`, the "two conventions both valid" framing, PR #74's URGENT inflection. Supersede banner trail: PR #75 (banners), PR #79 (self-ref fix #75 → #78).

**Post-purge v1.5 Brown apples-to-apples empirical numbers** (`docs/v1_5_brown_post_purge_numbers_2026-05-27.md`, run at `f1e9c81`, 272.91 s for both fixtures end-to-end):

| Fixture | L1 max | Top-action % | Shallow viol | Coverage | Strict max \|Δ\| | Verdict |
|---|---|---|---|---|---|---|
| `dry_K72_rainbow` | 1.703 (was 1.736) | 160/168 (95.2%) | 13 / 0 | 100.0% | **0.852** (was 0.868) | PASS (gated SANITY) |
| `dry_A83_rainbow` | 1.813 (unchanged) | 326/342 (95.3%) | 21 / 0 | 100.0% | **0.907** (was 0.907 — unchanged) | PASS (gated SANITY) |

K72 tightened slightly (~2% on L1 + ~2% on strict). A83 is unchanged at four-digit precision; this is the **Nash multiplicity floor**, NOT a convention bug. Both fixtures PASS the 4-layer reframed gate. Strict layer remains informational per `feedback_nash_multiplicity_acceptance.md`.

**Nash multiplicity (PR #68 confirmation persists):** at 2000 iters with identical RNG seed and `ε = 1e-9` regret-init perturbation, per-cell average strategies diverge by up to `max |Δ| = 0.998499` on indifference-manifold cells. This is the residual under fixed-convention conditions and structurally explains why the A83 strict-cell number doesn't collapse to zero post-purge. EV-of-action invariance is the testable invariant for the multiplicity zone (per `docs/ev_invariance_sanity_gauntlet_design_2026-05-27.md`); strategy-probability per-cell is non-falsifiable.

**Persona W4.3 (Priya — Brown apples-to-apples parity):** PASS-aggregator → **PASS-strict** (PR #83, `839645e`). Source: `docs/persona_post_purge_retest_2026-05-27.md` (agent `a3844485`). 276.45 s wall on both A83 + K72 board parametrizations under the canonical convention. Overall persona count unchanged at 10/4/2/1 (quality improvement, not count change).

**v1.6.1 hold-lift VALIDATED (PR #84, `a44c93e`):** the supersede banner on `docs/v1_6_1_ship_hold_review_2026-05-26.md` flipped from "PARTIALLY SUPERSEDED / incomplete rationale" → "VALIDATED". The hold-lift conclusion (A: lift the v1.6.1 ship-HOLD, fold into v1.8.0) stands and is now empirically confirmed by W4.3 strict-PASS post-purge.

**Ship-blocker repaired (PR #69, `98fb503`):** the silent no-op `chance_outcomes() = Vec::new()` path that produced the INVALIDATED v1 results now hard-fails. `validate_config` in `crates/cfr_core/src/hunl_solver.rs` rejects `initial_hole_cards.is_none()` for the scalar postflop path with `HUNLSolveError::InvalidConfig`. Python `_build_postflop_config` rejects `--backend rust` postflop CLI invocations without `--initial-hole-cards` with a `ValueError`. No behavior change for already-correct callers. Full RCA: `docs/chance_outcomes_empty_rca_2026-05-26.md`.

**Unified A83 story (post-purge, final):**
1. **DCFR math:** CORRECT (third-pass validator PASS).
2. **Terminal-utility convention:** PURGED — only canonical Brown formula remains. The "rust" convention IS A BUG (correcting the arbitrator's prior verdict).
3. **Reach-weighted regret-update bias (PR #93 ablation finding):** STRUCTURALLY IMPOSSIBLE going forward.
4. **Residual deep-cap divergence:** genuine **Nash multiplicity at deep-cap indifference manifolds** (PR #68 confirmation persists; `max |Δ| = 0.998499` under fixed-convention / ε-perturb).
5. **Brown vs Rust:** both correct under the canonical formula; in the multiplicity zone they can pick different Nash equilibria. Strategy-prob per-cell convergence is non-falsifiable; EV-of-action invariance is the load-bearing invariant.
6. **v1.6.1 hold-lift decision:** VALIDATED (PR #84).
7. **v1.5 Brown apples-to-apples reframed 4-layer gate:** PASSES on both A83 + K72 post-purge (`docs/v1_5_brown_post_purge_numbers_2026-05-27.md`).
8. **Persona W4.3:** PASS-strict (was PASS-aggregator).

**Math-error supersede banner** on `docs/a83_deep_cap_root_cause_investigation.md` §2 Candidate (d): **DONE via PR #63** (`533cb8e`); self-ref fix via PR #79 (`d1fcc3f`). Doc is now functionally + explicitly superseded by the empirical confirmation + convention purge.

---

**SUPERSEDED FRAMINGS (kept for audit-trail integrity):**

~~**Sixth-pass (~03:57 UTC 2026-05-27) framing:** convention purge IN FLIGHT on branch `pr-75-purge-rust-convention`; recovery agent `a20d5e8d` mid-test-rebaseline. Original purge agent `a73e761` was silently killed mid-test-debug.~~ — **Resolved:** the purge LANDED as PR #78 / `37e5be1` on 2026-05-27 ~04:15 UTC.

~~**Fifth-pass (~09:20 UTC 2026-05-26) framing:** A83 status IN FLUX post-PR-93-ablation. Both PR #68 (Nash multiplicity) and PR #93 (TU convention) findings real; reconciliation pending. v1.6.1 hold-lift + v1.8.0 ship UNDER REVIEW.~~ — **Resolved:** the convention purge made the reconciliation moot; the reach-weighted bias is structurally impossible post-purge, residual is the genuine multiplicity.

~~**Fourth-pass (~04:30 UTC 2026-05-26) framing:** Finding #1 FULLY CLOSED — terminal-utility arbitrator verdict NOT A BUG + Nash multiplicity confirmed. DCFR math correct, terminal-utility NOT a bug, Nash multiplicity confirmed. No further investigative action required.~~ — **Partially refuted:** the terminal-utility convention WAS a bug (PR #78 purged it). The Nash multiplicity confirmation is still correct + load-bearing.

### 2. v1.8 SIMD speedup — SETTLED: ~1.0× (release notes corrected via PR #56)

**FINAL STATUS:** Release-notes draft rewritten from "4-8× / 2-4× / 1.5-2×" to "no measurable wall-clock improvement on M4 Pro for typical postflop widths; SIMD provides a portability + correctness floor." This shipped to `origin/main` via PR #56 (`bf645ae`). No further action required.

**Root cause confirmed:** LLVM `-O3` autovectorizer already covers small-slice case (width 2-5) on arm64 M-series. Explicit SIMD wins (2-3×) only kick in at width 8+, which production code does not hit.

---

## Persona retest status (post-v1.8 + post-purge)

Aggregate from `docs/persona_status_post_v1_8_2026-05-26.md` + `docs/persona_post_purge_retest_2026-05-27.md` + subsequent retest PRs. **Current count: 10 PASS / 4 PARTIAL / 2 BLOCKED / 1 FAIL** (unchanged; W4.3 is a quality-strengthen, not a count change).

| Persona | Prior | Current | Type | PR / Source |
|---|---|---|---|---|
| W3.3 | PARTIAL | **PASS** | Closed | PR #66 (`c15b952`) — post-v1.8 P2 closing test. Post-purge re-verified (C1-C4 all PASS; game_value visible 5.0 → 10.0 BB constant-sum signature). |
| W1.5 | PARTIAL | PARTIAL (no change) | C-NICE | Not user-facing-blocking; retained as PARTIAL. |
| W2.4 | PARTIAL | PARTIAL (no change) | D | Out-of-scope per persona-test rectification framework. |
| W2.1 | PARTIAL | PARTIAL | River PASS / flop FAIL | River meets the bar; flop unchanged. |
| W4.3 | PASS-aggregator | **PASS-strict** | Strict-pass (post-purge upgrade) | PR #83 (`839645e`) — `docs/persona_post_purge_retest_2026-05-27.md` (agent `a3844485`) confirmed `tests/test_v1_5_brown_apples_to_apples.py` PASSES both A83 + K72 (276.45 s wall) under canonical Brown convention. |
| W3.2 | BLOCKED | PASS | Closed | PR #59 (`6d99a96`) + PR #56 BR-smoke. Post-purge re-verified — `exploit_gap >= 0` invariant holds on Kuhn (6/6) + HUNL `default_tiny_subgame` both seats. |
| W3.4 | BLOCKED | PASS (caveated) | Closed-with-caveat | PR #59; river-repurposed fixture. |
| W3.5 | PARTIAL | PARTIAL (no change) | C — no v1.8 regression | PR #59. |
| W4.2 | — | RETEST IN FLIGHT (`a545b89`) | TBD | In-flight sub-agent; > 25 hr old, likely stalled per [agent-execution-timeout](feedback_agent_execution_timeout.md). |

**Post-purge convention impact (per `docs/persona_post_purge_retest_2026-05-27.md`):** convention change shifted absolute `game_value` (+`initial_pot/bb` per leaf, e.g., 5.0 → 10.0 BB on `default_tiny_subgame`) but did NOT shift **relative strategy** or violate **`exploit_gap >= 0` invariant**. Kuhn personas bit-identical (no dead money → constant-sum reduces to zero-sum). W2.3 remains BLOCKED on perf, not utility convention. The W4.3 reclassification (aggregator → strict) is the only category change.

References: `docs/persona_status_post_v1_8_2026-05-26.md`, `docs/persona_post_purge_retest_2026-05-27.md`, `docs/persona_w3_2_smoke_2026-05-26.md`, [persona-test-rectification](feedback_persona_test_rectification.md), [persona-time-budgets](feedback_persona_time_budgets.md).

---

## What landed on `origin/main` this session

All merges 2026-05-26 UTC, verified against `git log origin/main --since="2026-05-25 22:00"`:

### First burst (pre-RESUME-draft; PRs #39-#48)

| Merge SHA | PR # | Title |
|---|---|---|
| `5ead08f` | #47 | `fix(dmg): arch label + version stamp accuracy (post-fork-bomb-fix follow-up)` |
| `77e751c` | #32 | `feat(cfr_core): v1.8 Phase 4 — compute_strategy SIMD` |
| `610986d` | #48 | `docs(usage): refresh §7b version label + add regime guidance summary` |
| `feee974` | #38 | `feat: exploitative play — best-response vs fixed opponent (PR 76)` |
| `9dbe7ff` | #36 | `docs: spec for v1 Range fractional-frequency refactor` |
| `7443151` | #46 | `fix(types): resolve 7 substantive mypy errors (post-cleanup follow-up)` |
| `dbfc8d0` | #45 | `docs: drift cleanup v2 — USAGE / CHANGELOG / dmg_install_guide` |
| `a6b89f7` | #44 | `docs: fix executable code bugs in README + USAGE` |
| `a712950` | #33 | `feat(cfr_core): v1.8 Phase 3 — update_strategy_sum SIMD` |
| `cfc6bc5` | #43 | `chore: green up lint/clippy/format/deps gates on main` |
| `728206e` | #42 | `fix(dmg): add multiprocessing.freeze_support() to PyInstaller entry + warn users` |
| `8073bcc` | #41 | `PR 63b: v1.8 Phase 2 — update_regret_sum SIMD` |
| `79d6534` | #40 | `docs: persona test status snapshot 2026-05-25` |
| `1658698` | #39 | `fix(ci): gate Golden File Check require-step on real detector output` |

### Second burst (post-RESUME-draft; PRs #50-#60)

| Merge SHA | PR # | Title | Notes |
|---|---|---|---|
| `eb74fb3` | #60 | `docs: track previously-orphan reference targets (v1_7_1_tag_decision + others)` | Backfills 8 docs cited from tracked README/CHANGELOG/USAGE but only living as untracked workspace files. Verbatim track only. **Note:** `a83_deep_cap_root_cause_investigation.md` tracked with known math error in §2(d); supersede banner added separately via PR #63. |
| `6d99a96` | #59 | `docs: persona status update post-W3.2/W3.4 retests (post-v1.8 SIMD)` | Reflects W3.2 BLOCKED→PASS (PR 76 BR API), W3.4 BLOCKED→PASS-on-repurposed-river-fixture (caveated), W3.5 PARTIAL unchanged (no regression from v1.8). |
| `9948eaa` | #58 | `docs: add CHANGELOG note for poker-solver shim quirk (workaround documented)` | Small CHANGELOG entry for the `.dmg` `poker-solver` PATH shim quirk; workaround documented. |
| `0d1c717` | #57 | `docs: resolve 7 MEDIUM stale claims (post-HIGH-fix follow-up)` | Doc-staleness sweep follow-up after PR #55 cleared the 8 HIGH-severity stale claims. |
| `bf645ae` | #56 | `docs: v1.8 release notes honesty (~1.0x not 4-8x) + W3.2 BR smoke` | **Major Finding #2 settled.** Release notes draft rewritten to honest framing. W3.2 BR-API smoke added. |
| `7bb21d8` | #55 | `docs: resolve 8 HIGH stale claims (v1.7.2→v1.8.0 fold, A83 closure, v1.6.1 HOLD lifted)` | Top-priority sweep across README, CHANGELOG, USAGE, audit docs. |
| `97886e1` | #54 | `feat: v1.8.0 release execution script + .dmg build runbook` | Adds `scripts/release_v1_8_0.sh` (hard-fail `set -euo pipefail` runbook) + `docs/dmg_build_runbook_2026-05-26.md`. **Ship script is ready to invoke.** |
| `f165eb8` | #52 | `docs: archive 34 unreferenced 2026-05-23/25 session drafts` | Workspace cleanup; orphan docs moved to `docs/_archive_2026-05-26_session/`. |
| `29d608e` | #53 | `feat: --regret-init-noise flag for Nash-multiplicity testing (A83 Track A)` | CLI flag + Rust wiring. **v1 nohup probe runs INVALIDATED; superseded by correct-entrypoint probe in PR #68.** |
| `3eea3b1` | #51 | `docs: persist terminal-utility audit + arbitration (A83 investigation)` | **Major Finding #1 half-a settled.** Position A audit + arbitration verdict + Position B validator all tracked. |
| `a5be2be` | #50 | `docs: persist A83 validation + update v1.8.0 release notes + lift v1.6.1 hold framing` | First post-RESUME-draft merge. Lifts v1.6.1 ship-HOLD; folds into v1.8.0. |

### Third burst (post-RESUME-third-pass; PRs #61-#69)

| Merge SHA | PR # | Title | Notes |
|---|---|---|---|
| `b401f6c` | #68 | `feat: A83 Nash multiplicity empirically CONFIRMED via corrected probe` | **Finding #1 half-b SETTLED.** Adds `scripts/a83_nash_multiplicity_probe.py` (correct vector-form entrypoint) + `docs/a83_nash_multiplicity_confirmed_2026-05-26.md`. Verdict: `max |Δ| = 0.998499` → NASH-MULTIPLICITY. v1.8.0 release notes Known-Issues entry updated to "empirically confirmed". |
| `98fb503` | #69 | `fix(solver): hard-fail scalar HUNL postflop without initial_hole_cards (v1.8 ship-blocker)` | **v1.8 ship-blocker hot-patch.** Closes the silent `chance_outcomes()` empty-list path that produced INVALIDATED v1 results. `HUNLSolveError::InvalidConfig` + `ValueError` in CLI. Full RCA at `docs/chance_outcomes_empty_rca_2026-05-26.md`. |
| `d0b7b34` | #67 | `docs: surface pyenv x86_64 vs arm64 .so arch hazard for devs` | Adds onboarding hazard doc for the x86_64-on-arm64 silent-SKIP issue (a known memory-flagged hazard). |
| `c15b952` | #66 | `docs: persona W3.3 PARTIAL->PASS (post-v1.8 P2 closing test)` | W3.3 retest closed PARTIAL→PASS. |
| `c46621c` | #65 | `docs: surface Gate 4 + Track A invalidation; propose correct A83 probe` | Surfaced the `chance_outcomes()` invalidation; proposed the correct-entrypoint probe that became PR #68. |
| `8fa5b5b` | #64 | `docs(readme): mark ad-hoc postflop CLI as multi-minute; keep tiny_subgame as Quick-start` | README HUNL CLI warning; preserves `tiny_subgame` as quick-start. |
| `533cb8e` | #63 | `docs: supersede banner on a83 RC investigation (math error in §2(d))` | Banner added to `docs/a83_deep_cap_root_cause_investigation.md` flagging the §2(d) math error; doc is now functionally + explicitly superseded by PR #68 confirmation. |
| `1e4e60c` | #62 | `docs: v1.8.0 release notes final polish (persona + arbitrator + cleanup wave)` | Polish pass on release notes. |
| `55122c3` | #61 | `docs: fix 2 broken reference paths (dcfr_brown_2019.pdf, code/noambrown path)` | Broken-ref repair. |

### Fourth burst (mid-AFK, 2026-05-26 ~09:30 UTC → 2026-05-27 ~04:00 UTC; PRs #70-#76)

| Merge SHA | PR # | Title | Notes |
|---|---|---|---|
| `9bd071f` | #76 | `docs: archive shipped wave logs + R1-R11 narrative from PLAN.md (split-out)` | Pre-prune PLAN.md content archived at `docs/_archive_plan_2026-05-27/`. PR title in commit log reads "archive pre-prune PLAN.md content" — same change. Live PLAN.md prune is now safe to land. |
| `44a9ee5` | #75 | `docs: supersede banners on retracted A83/terminal-utility docs (PR #75 purges 'rust' convention)` | Supersede banners on A83 / terminal-utility docs flagging the prior "Nash multiplicity-only" framing as superseded by the convention purge. PR #75 was the banner PR; PR #78 is the actual code-level purge. |
| `3b9bd07` | #74 | `🚨 docs: A83 terminal-utility ablation contradicts arbitrator — needs reconcile` | Surfaced `docs/A83_RECONCILE_URGENT_2026-05-26.md` (reconcile hypothesis + user decisions). Reconciliation made moot by PR #78. |
| `eaca561` | #73 | `docs: README + USAGE final consistency polish (6 findings)` | Polish pass on README + USAGE post-A83 reframe. |
| `0f290a8` | #72 | `docs: final integrity sweep — surface 3 morning-handoff artifacts` | Surfaces 3 morning-handoff artifacts to main. |
| `c76aff8` | #71 | `docs: final consistency fix-up on v1.8.0 release notes + CHANGELOG` | Release-notes + CHANGELOG fix-up. |
| `fa83413` | #70 | `chore(gitignore): ignore transient session artifacts (release-script unblock)` | Adds `.gitignore` entries for transient session artifacts that were blocking clean-tree checks in the release script. |

### Fifth burst (post-purge, 2026-05-27 ~04:00 UTC → ~09:30 UTC; PRs #77-#84 — KEYSTONE BURST)

| Merge SHA | PR # | Title | Notes |
|---|---|---|---|
| `a44c93e` | #84 | `docs: v1.6.1 hold-lift VALIDATED post-purge (W4.3 strict-PASS confirms)` | Banner on `docs/v1_6_1_ship_hold_review_2026-05-26.md` flipped "PARTIALLY SUPERSEDED" → "VALIDATED". Hold-lift conclusion (A) empirically confirmed by W4.3 strict-PASS post-purge. |
| `839645e` | #83 | `docs: persona W4.3 reclassified PASS-strict post-convention-purge` | W4.3 (Priya Brown apples-to-apples parity) PASS-aggregator → PASS-strict. Source: `docs/persona_post_purge_retest_2026-05-27.md` (agent `a3844485`); `tests/test_v1_5_brown_apples_to_apples.py` PASSES both A83 + K72 (276.45 s wall) under canonical convention. Overall count unchanged at 10/4/2/1. |
| `f1e9c81` | #82 | `test: add @pytest.mark.slow to 2 more heavy tests in test_river_diff_self_sanity (follow-up to b53e0f0)` | macos-14 Tier 1 fix: prior @slow-marker PR missed `test_strategy_matrix_shape` + `test_iterations_override_respected` in `test_river_diff_self_sanity.py`. Both >300s timeout candidates. Markers already registered in `pyproject.toml`. |
| `9846827` | #81 | `docs: substitute PR #78 + SHA 37e5be1 in v1.8.0 release notes (post-purge known values)` | Replaces `<TBD-PR-NUMBER>` → `78` and `<TBD-MERGE-SHA>` → `37e5be1` in `docs/v1_8_0_release_notes_DRAFT.md`. 2 of 4 TBDs filled; 2 validator-dependent ones (`<TBD-POST-PURGE-RESIDUAL>`, `<TBD-POST-PURGE-MAX-Δ-A83-2K>`) intentionally left for the final-substitution PR. |
| `a824556` | #80 | `feat(release): one-shot v1.8.0 ship trigger wrapper` | Adds `scripts/release_v1_8_0_trigger.sh` — single-command wrapper around `scripts/release_v1_8_0.sh`. Dynamically reads origin/main HEAD SHA (overrides inner script's stale EXPECTED_SHA); stashes ~274 untracked docs for clean-tree check; hard-fails BEFORE inner script on post-purge preconditions (canonical formula in `hunl.rs`; zero TBDs in release notes; v1.5 Brown test exists). `--dry-run` flag stops before tag/push/release. `--skip-post-purge-checks` escape hatch. Defends against `pipefail`+`grep -q` SIGPIPE hazard. Runbook: `docs/v1_8_0_ship_trigger_runbook_2026-05-27.md`. |
| `d1fcc3f` | #79 | `docs: fix self-ref in retracted-doc supersede banner (PR #75 -> PR #78)` | Surgical replacement: the 2026-05-27 ADDENDUM in `a83_deep_cap_root_cause_investigation.md` referenced "PR #75" as the convention-purge PR, but PR #75 was the banner PR itself (self-reference). Actual purge PR is #78. |
| **`37e5be1`** | **#78** | **`fix(engine): purge 'rust' terminal-utility convention; canonical Brown formula is the only utility`** | **KEYSTONE PR.** Empirical purge: no `TerminalUtilityConvention` enum, no `terminal_utility_convention` field on `HUNLConfig`, no per-leaf branching. 8 files / +214/-85 — engine (`hunl.rs`, `exploit.rs`, `hunl.py`) + tests (5 files rebaselined incl. pre-existing broken asserts in `test_03` + `test_06` from PR 35 Fix C drift). Game now constant-sum: `u[0]+u[1] = initial_pot/big_blind`. Retracts `docs/terminal_utility_arbitration_2026-05-26.md` + "two conventions both valid" framing + PR #74's URGENT inflection. |
| `d5f17b3` | #77 | `docs: pre-fill v1.8.0 release notes for post-purge ship (TBD placeholders flagged)` | Pre-fills `docs/v1_8_0_release_notes_DRAFT.md` with the post-purge ship narrative; 4 TBD placeholders explicitly flagged (PR #, SHA, residual, max-Δ). |

**Total this session window:** **49 merges** (#39-#84 + #32, #33, #38). Branches all deleted on merge except those held for archive per audit policy.

---

## Open PRs left on origin

| PR # | Branch | Title | Status | Why open |
|---|---|---|---|---|
| #49 | `pr-92-resume-doc` | `docs: RESUME_2026-05-26 morning hand-off` | OPEN | **This PR.** Held for user review; do not auto-merge. |
| #20 | `pr-64-cross-platform-ci-matrix` | `feat(ci): cross-platform CI matrix for v1.8 prep` | OPEN | Rebased + `@pytest.mark.slow` markers added on heavy convergence tests (`b53e0f0` + PR #82 follow-up). Status at this update: 3 pre-existing test bugs identified for Tier 2 user decision; tracked separately. Per task brief "never auto-merge CI infra"; surfaced for your review. |
| **(in-flight)** | TBD | **Final TBD-substitution PR** (post-purge residual + max-Δ-A83-2k) | **GATING v1.8.0 SHIP** | Replaces the 2 remaining validator placeholders in `docs/v1_8_0_release_notes_DRAFT.md` (`<TBD-POST-PURGE-RESIDUAL>` + `<TBD-POST-PURGE-MAX-Δ-A83-2K>`). Empirical values are KNOWN per `docs/v1_5_brown_post_purge_numbers_2026-05-27.md`: residual = "L1 max 1.703 (K72) / 1.813 (A83); strict max \|Δ\| 0.852 / 0.907; both fixtures PASS under gated SANITY"; max-Δ-A83-2k = `0.907`. **Ship trigger wrapper HARD-FAILs while any TBD remains** — this is the last gate. |

Once the final TBD-substitution PR lands, `bash scripts/release_v1_8_0_trigger.sh --dry-run` should clear all preconditions, then `bash scripts/release_v1_8_0_trigger.sh` fires v1.8.0 ship.

---

## In-flight when this RESUME UPDATE was written

### Active worktrees (with uncommitted/unpushed work)

| Worktree | Branch | What it's doing | Expected output |
|---|---|---|---|
| `pr-92-resume-doc` | `pr-92-resume-doc` | **This RESUME doc** (eighth-pass refresh). | This document; PR #49. |
| `pr-75-canonical-utility` | `pr-75-purge-rust-convention` | Convention purge worktree (now LANDED as PR #78 / `37e5be1`). | Safe to clean — purge merged to main. |
| `pr-79-fix-self-ref` | `pr-79-fix-self-ref` | Self-ref fix for retracted-doc banner (now LANDED as PR #79 / `d1fcc3f`). | Safe to clean — merged. |
| `pr-80-slow-markers` | `pr-80-slow-markers` | @slow markers follow-up (now LANDED as PR #82 / `f1e9c81`). | Safe to clean — merged. |
| `pr-88-v1.8.0-notes` | `pr-88-v1.8.0-release-notes-prep` | v1.8.0 release-notes draft. Branch lags main; **safe to recreate from origin/main if needed.** | The published `docs/v1_8_0_release_notes_DRAFT.md` on main is the canonical version. Branch can be closed. |
| `pr-93-tu-ablation` | `pr-93-terminal-utility-ablation` | Terminal-utility ablation runs (PR #93 — feature-flag obsolete post-purge). | Decide: close as obsolete (convention purge made the flag moot) or merge as backwards-compat testing flag. |
| `pr-20-timeout` | `pr-20-rebase-with-timeout-fix` | Retained for inspection of PR #20 (CI matrix) Leduc-diff Linux timeout fix attempt. | inspection-only |
| `pr-20-slow-markers` | (variant) | PR #20 slow-marker variant. | inspection-only |
| `rebase-pr-20` | `rebase-20-2026-05-26` | Post-rebase worktree retained for inspection of PR #20. HEAD `2226c6d`. | inspection-only |

**Worktree count: 9.** Several are post-merge cleanable; convention-purge worktree (`pr-75-canonical-utility`) is safe to remove now that PR #78 has merged.

### In-flight sub-agents (as of 2026-05-27 ~09:30 UTC eighth-pass refresh)

| Agent ID | Job | Status |
|---|---|---|
| (TBD) | **Final TBD-substitution PR.** Replaces 2 remaining validator placeholders in `docs/v1_8_0_release_notes_DRAFT.md` with known values from `docs/v1_5_brown_post_purge_numbers_2026-05-27.md`. **GATES v1.8.0 ship.** | IN FLIGHT (per task brief) |
| `a20d5e8d` | Convention-purge recovery agent. | **DONE** — LANDED as PR #78 (`37e5be1`) on 2026-05-27 ~04:15 UTC. Worktree `pr-75-canonical-utility` safe to clean. |
| `a3844485` | Post-purge persona retest sweep. | **DONE** — produced `docs/persona_post_purge_retest_2026-05-27.md`; fed into PR #83 (W4.3 reclass). |
| `afef13e` | Post-purge 2k iter validation. | **DONE** (or rolled into the v1.5 Brown post-purge numbers capture at `docs/v1_5_brown_post_purge_numbers_2026-05-27.md`) — A83 / K72 numbers in hand. |
| `ae70403` | Perpetual QA iter 2. | IN FLIGHT (likely — iter 1 was 8/8 PASS per `docs/perpetual_qa_findings_2026-05-27.md`). |
| `a545b89` | W4.2 persona retest (carry-over from third burst). | IN FLIGHT (possibly stalled — > 26 hr old at this update; flag for cleanup per [agent-execution-timeout](feedback_agent_execution_timeout.md)). |
| `acebb72f` | A83 terminal-utility ablation (PR #93). | DONE — feature-flag obsolete post-purge; decide on PR #93 disposition. |

All other sub-agents from this session have either finished and merged or been pruned.

### Background nohup jobs (historical — INVALIDATED + REPAIRED)

| PID | Job | Status |
|---|---|---|
| `34053` (historical) | Gate 4 200K v2 (river DONE + turn IN-PROGRESS at iter ?/200000) | **INVALIDATED** per `chance_outcomes()` empty-list bug (now hard-failed via PR #69). River output was a 186-byte empty-strategy fallback; turn phase killed at ~60 min mid-run. See Invalidated experiments section. |
| `57317` (historical) | A83 Track A K72 @ 2000 iters Rust + Brown arms (v1 nohup probe) | **INVALIDATED** (same root cause). Superseded by `scripts/a83_nash_multiplicity_probe.py` (PR #68) using the correct `solve_range_vs_range_rust` vector-form entrypoint. |
| `57948` (historical) | W2.3 v1.8 retest | Output at `/tmp/w23_v18_retest_output.log`; result already folded into persona retest tracker. |

---

## TODO list status (current task set, 37 tasks; #38 added post-fifth-burst)

> **HIGHEST-PRIORITY TASK (#38):** Final TBD-substitution PR — replace `<TBD-POST-PURGE-RESIDUAL>` + `<TBD-POST-PURGE-MAX-Δ-A83-2K>` in `docs/v1_8_0_release_notes_DRAFT.md` with known values from `docs/v1_5_brown_post_purge_numbers_2026-05-27.md`. **GATES v1.8.0 ship.** Values: residual = "L1 max 1.703 (K72) / 1.813 (A83); strict max \|Δ\| 0.852 / 0.907; both fixtures PASS under gated SANITY"; max-Δ-A83-2k = `0.907`.


| # | Task | Status |
|---|---|---|
| 1 | Merge v1.8 SIMD Phase 1-4 to main | DONE (#23, #35, #41, #33, #32) |
| 2 | `.dmg` fork-bomb fix to main | DONE (#42) |
| 3 | `.dmg` packaging hardening (arch / version / codesign) | DONE (#47) |
| 4 | Lint / clippy / format / deps green-up | DONE (#43) |
| 5 | mypy follow-up (7 substantive type errors) | DONE (#46) |
| 6 | Doc-code-bug fixes in README + USAGE | DONE (#44) |
| 7 | Doc-drift cleanup v2 | DONE (#45) |
| 8 | Golden File Check CI gating fix | DONE (#39) |
| 9 | Refresh merge refs for PR #20, #24, #36, #38 | DONE for #36/#38; PR #24 closed-obsolete; #20 rebased and surfaced |
| 10 | PR #24 obsolescence decision | DONE — closed, salvage as PR #48 |
| 11 | v1.7.1 tag decision | DONE — CLOSE-as-obsolete; folded into v1.8.0 |
| 12 | v1.6.1 ship-or-hold review | DONE — HOLD LIFTED; folded into v1.8.0 (PR #50 framing) |
| 13 | v1.8.0 release notes draft | DONE — on main with honest SIMD framing (PR #50 + #56) + A83 settlement |
| 14 | v1.8.0 tag + GitHub release | **READY TO INVOKE** — `scripts/release_v1_8_0.sh` on main (PR #54). User decision: invoke when ready. |
| 15 | Backup/integration sync with origin/main | PARTIALLY STALE — backup at `5ead08f` is 19 commits behind. Re-sync queued (next session start). |
| 16 | Rebuild `.dmg` locally with v1.8.0 build | NOT DONE — explicitly queued LAST per session order. Runbook at `docs/dmg_build_runbook_2026-05-26.md` (PR #54). |
| 17 | Backup-branch audit (10 stale `backup/pr-*` branches) | DONE — report at `docs/backup_branch_audit_2026-05-26.md`; 5 DELETE + 5 SURFACE; no deletes executed (awaiting your opt-in) |
| 18 | Surviving-worktree audit + cleanup | DONE — 5 removed; salvage archived; **worktree count went 23→9** |
| 19 | DCFR math 3rd validator audit | DONE — `docs/a83_validation_2026-05-26.md` (now on main via PR #50/#51) |
| 20 | Terminal-utility audit (verify or refute BUG claim) | **DONE — verdict NOT A BUG; persisted on main via PR #51.** Supersede banner on `a83_deep_cap_root_cause_investigation.md` §2(d) added via **PR #63**. **PR #68** adds the corrected `solve_range_vs_range_rust` probe + `docs/a83_nash_multiplicity_confirmed_2026-05-26.md` confirming Nash multiplicity (`max |Δ| = 0.998499`) on the same A83 fixture — this empirically backs the NOT-A-BUG arbitrator verdict from the multiplicity side. **Ship-blocker hot-patch (PR #69)** closes the silent `chance_outcomes()` no-op path so future invocations hard-fail instead of returning empty strategies. **Both halves of the A83 investigation are now FULLY CLOSED with empirical backing + ship-blocker repaired.** |
| 21 | PLAN.md prune | DONE on branch `pr-89-plan-prune`; **local write-back HELD** per `plan_prune_pr_89_2026-05-26.md`. |
| 22 | Memory directory prune | DONE — **30 → 25 entries**; consolidations applied |
| 23 | Gate 4 200K turn invocation reconstruction | **INVALID — see Invalidated experiments section below.** River-phase v2 produced empty-strategy output (186-byte log, uniform-random fallback). Turn phase was killed by orchestrator at ~60 min mid-run. **Future invocations of the silent no-op path are now hard-failed by PR #69**; re-run is optional (v1.5 Brown acceptance baseline already covers v1.8.0 release boundary). |
| 24 | 8 HIGH-severity doc-staleness fixes | DONE (PR #55) |
| 25 | 7 MEDIUM-severity doc-staleness fixes | DONE (PR #57) |
| 26 | Orphan reference-doc backfill (8 docs) | DONE (PR #60) |
| 27 | A83 Track A `--regret-init-noise` flag (nohup probe runs) | **DONE — corrected probe shipped 2026-05-26 ~04:13 UTC.** The v1 200K nohup runs are INVALID (see Invalidated experiments section); replaced by `scripts/a83_nash_multiplicity_probe.py` (PR #68), which uses the correct `solve_range_vs_range_rust` vector-form entrypoint and the v1.5 Brown apples-to-apples 2000-iter configuration. **Verdict: NASH-MULTIPLICITY CONFIRMED** (`max |Δ| = 0.998499` overall; ~25-28% on the bottom-pair-Ace cluster). See `docs/a83_nash_multiplicity_confirmed_2026-05-26.md` for full evidence. The `--regret-init-noise` flag implementation on main remains correct and is now empirically load-bearing. |
| 28 | Persona retest post-v1.8 (W3.2, W3.4, W3.5) | DONE — W3.2 BLOCKED→PASS, W3.4 BLOCKED→PASS-on-repurposed-fixture (caveated), W3.5 PARTIAL unchanged. PR #59 published. Subsequent: W3.3 PARTIAL→PASS via PR #66. |
| 29 | v1.8 ship-blocker hot-patch (`chance_outcomes()` empty-list silent no-op) | **DONE** — PR #69 (`98fb503`). `validate_config` + CLI both hard-fail on missing `initial_hole_cards` for scalar postflop Rust path. RCA: `docs/chance_outcomes_empty_rca_2026-05-26.md`. |
| 30 | A83 Nash multiplicity correct-entrypoint probe | **DONE** — PR #68 (`b401f6c`). Verdict: NASH-MULTIPLICITY CONFIRMED (`max |Δ| = 0.998499`). Doc: `docs/a83_nash_multiplicity_confirmed_2026-05-26.md`. |
| 31 | Orphan/broken reference cleanups + release-notes polish + supersede banner | DONE — PR #61 (broken refs), PR #62 (release-notes polish), PR #63 (supersede banner), PR #64 (README HUNL warning), PR #65 (invalidation surface), PR #67 (pyenv hazard). |
| 32 | Persona W4.2 retest | IN FLIGHT (`a545b89` sub-agent at time of this update). |
| **33** | **A83 reconcile (PR #68 Nash multiplicity vs PR #93 TU convention ablation)** | **DONE — reframed via convention purge.** PR #74 (`3b9bd07`) surfaced the reconcile. Decision was: purge the "rust" convention so Brown's becomes canonical/only. Convention purge LANDED as PR #78 (`37e5be1`). Post-purge: residual deep-cap divergence IS the genuine Nash multiplicity (PR #68 confirms); convention bias is now structurally impossible. |
| **34** | **Convention purge (the keystone)** | **DONE — PR #78 (`37e5be1`).** 8 files / +214/-85. Engine + Python + 5 test files rebaselined. Retracts terminal-utility arbitration verdict + "two conventions both valid" framing. Game is now constant-sum: `u[0]+u[1] = initial_pot/big_blind`. |
| **35** | **Post-purge v1.5 Brown apples-to-apples validation** | **DONE — captured at `docs/v1_5_brown_post_purge_numbers_2026-05-27.md`.** K72: L1 max 1.736→1.703, strict max \|Δ\| 0.868→0.852 (~2% tighter). A83: unchanged at 1.813 / 0.907 (Nash multiplicity floor). Both PASS gated SANITY. |
| **36** | **Perpetual QA iter 2** | IN FLIGHT (agent `ae70403`). Iter 1 was 8/8 PASS (`docs/perpetual_qa_findings_2026-05-27.md`). |
| **37** | **PR #20 CI matrix** | Pre-existing test bugs identified for Tier 2 user decision (3 bugs). `@pytest.mark.slow` markers added on heavy convergence tests (`b53e0f0` + PR #82 follow-up). Awaiting user disposition. |
| **38** | **Final TBD-substitution PR (gates v1.8.0 ship)** | IN FLIGHT. Replaces `<TBD-POST-PURGE-RESIDUAL>` + `<TBD-POST-PURGE-MAX-Δ-A83-2K>` in `docs/v1_8_0_release_notes_DRAFT.md` with known values from #35. Ship trigger wrapper (PR #80) hard-fails until zero TBDs remain. |

---

## What did NOT land that you might expect

- **v1.7.1 tag and release.** CLOSE-as-obsolete. Folded into v1.8.0.
- **v1.8.0 tag and GitHub release.** Ship script (`scripts/release_v1_8_0.sh`, PR #54) + ship trigger wrapper (`scripts/release_v1_8_0_trigger.sh`, PR #80) are on main + executable. Awaiting final TBD-substitution PR + your invocation. Dry-run VERIFIED.
- **`.dmg` rebuild + re-upload.** Queued as Task #16, intentionally LAST. Runbook ready at `docs/dmg_build_runbook_2026-05-26.md`.
- **Local PLAN.md write-back.** Pruned version on branch `pr-89-plan-prune`; live PLAN.md is gitignored at 48.9 KB (was 102 KB).
- **PR #20 merge.** CI matrix rebased but not merged per "never auto-merge CI infra." 3 pre-existing test bugs identified for Tier 2 user disposition.
- **Backup-branch deletes + surface decisions.** Per `backup_branch_audit_2026-05-26.md`; awaiting opt-in.
- **`scripts/ship_v1_7_1.sh` archive.** Per v1.7.1 decision Action #4: move to `scripts/archive/`. Not done.
- **PR #93 disposition.** Now obsolete post-purge (convention-toggle flag has no convention to toggle). Decide: close or merge as backwards-compat regression-toggle.

*(Note: convention purge — previously the keystone blocker — LANDED as PR #78 / `37e5be1`. Removed from this list.)*

---

## Invalidated experiments (2026-05-26 morning — surfaced post-burst; subsequently REPAIRED)

**Two experiments that completed during the session window are NOT
valid results.** The underlying silent no-op path is now **HARD-FAILED**
via **PR #69** (`98fb503`) — future invocations of the same shape will
return `HUNLSolveError::InvalidConfig` / `ValueError` instead of empty
strategies. The Nash multiplicity question (the substantive reason for
the v1 nohup probe) was answered separately via **PR #68** using the
correct vector-form entrypoint. Source-of-truth analysis:
`docs/a83_track_a_results_analysis_2026-05-26.md` (agent `aaf54c9`) +
`docs/chance_outcomes_empty_rca_2026-05-26.md` (full RCA).

### 1. Gate 4 200K v2 (river + turn) — INVALID

- **River phase (v2):** Completed with a 186-byte empty-strategy log
  output at `/tmp/gate4_200k_v2_river.log`. Reported game-value /
  exploitability figures are uniform-random fallback, NOT a converged
  Nash equilibrium.
- **Turn phase (v2):** Killed mid-run by orchestrator at ~60 min
  wall-clock. `/tmp/gate4_200k_v2_turn.log` is 0 bytes.
- **Root cause:** `solve --hunl-mode postflop --backend rust`
  invocation WITHOUT `--initial-hole-cards`. The CLI constructs an
  `HUNLConfig` with `initial_hole_cards = None`, which causes
  `HUNLState::chance_outcomes` (in
  `crates/cfr_core/src/hunl.rs:533-568`) to return `Vec::new()`
  defensively. The scalar CFR loop's chance branch then iterates over
  the empty list, returns `[0.0, 0.0]` immediately, and never inserts
  a single infoset into `strategy_sum`. The 200,000 iters of
  "return [0,0]" complete in ~111 sec wall-clock. Reported
  exploitability is then recomputed by `_rust.compute_exploitability`
  against the empty strategy map and falls back to `uniform(n_actions)`
  on every cache miss (`exploit.rs:472-475`).
- **Note on the 2026-05-25 Gate 4 v1 result (5.28e-14 mbb/g
  river exploitability, monotone clean):** that earlier run used the
  same CLI shape and may have hit the same no-op path. **Treat as
  PROVISIONAL until re-run via the correct entrypoint.**

### 2. A83 Track A `--regret-init-noise` 200K nohup probe — INVALID

- **The 200K-iter nohup runs (baseline `noise=0.0` + perturbed
  `noise=1e-9`) BOTH hit the same no-op path described above.** Both
  log files are 186 bytes and byte-identical. Both completed in
  ~111 sec wall-clock (real 200K DCFR on a non-trivial tree takes
  tens of minutes).
- **The "bit-identical baseline+perturbed" outcome earlier reported
  was the trivial bit-identicality of two no-op runs, NOT evidence
  of Nash convergence on a unique equilibrium.**
- **CRITICAL distinction:** The `--regret-init-noise` flag
  IMPLEMENTATION (PR #53, commit `29d608e`) is CORRECT. Three unit
  tests in `crates/cfr_core/src/dcfr_vector.rs::tests` PASS,
  including a load-bearing `regret_init_noise_epsilon_perturbs_strategy`
  test on a multi-action tree (`cap=3 + include_all_in`). The CLI
  INVOCATION PATH the nohup script used is what's broken.
- **The Nash multiplicity hypothesis is now EMPIRICALLY CONFIRMED**
  (NOT UNTESTED, as the original invalidated-experiment text said).
  See PR #68 (`b401f6c`) + `docs/a83_nash_multiplicity_confirmed_2026-05-26.md`.
  Verdict: `max |Δ| = 0.998499` via the correct
  `solve_range_vs_range_rust` vector-form entrypoint.

### Recommended follow-up — DONE

- **Corrected probe:** SHIPPED via `scripts/a83_nash_multiplicity_probe.py`
  (PR #68). Verdict: NASH-MULTIPLICITY. 5.4 min wall-clock at 2000 iters
  on M4 Pro.
- **Silent no-op path hard-fail guard:** SHIPPED via PR #69
  (`crates/cfr_core/src/hunl_solver.rs::validate_config` +
  `poker_solver/cli.py::_build_postflop_config`).
- **Gate 4 200K re-run via correct entrypoint:** OPTIONAL — v1.5 Brown
  apples-to-apples acceptance baseline at `docs/v1_6_1_dryrun_10.md`
  already satisfies the v1.8.0 release boundary; the silent no-op
  ship-blocker is now repaired. Re-run only if you want the high-iter
  sanity check; see `docs/a83_followup_correct_experiment_2026-05-26.md`
  for the corrected-probe pattern.

---

## Recommended morning workflow (revised 2026-05-27 ~09:30 UTC — v1.8.0 ship-ready)

**STATE SUMMARY:** Convention purge LANDED (PR #78). W4.3 strict-PASS. v1.6.1 hold-lift VALIDATED. v1.5 Brown apples-to-apples PASS on both A83 + K72. v1.8.0 ship trigger wrapper VERIFIED via `--dry-run`. Only the final TBD-substitution PR is between you and v1.8.0 ship.

1. **Read this RESUME doc.** You're doing it now. Canonical state is at top of file; older passes preserved below for audit-trail integrity.
2. **`git pull --ff-only origin main`.** Sync local main to `a44c93e` (or downstream if the final TBD-substitution PR has landed since this update).
3. **Final TBD-substitution PR landed?** Check `gh pr list --state merged --limit 5 | head`. If yes → ship v1.8.0 (step 5). If no → wait for it to land (it's the last gate; values are known per `docs/v1_5_brown_post_purge_numbers_2026-05-27.md`: residual = "L1 max 1.703 (K72) / 1.813 (A83); strict max \|Δ\| 0.852 / 0.907; both fixtures PASS under gated SANITY"; max-Δ-A83-2k = `0.907`).
4. **Verify ship-readiness via dry-run.** `bash scripts/release_v1_8_0_trigger.sh --dry-run` from main. Hard-fails on:
   - Canonical formula heuristic in `crates/cfr_core/src/hunl.rs` (`contrib_subgame` / `initial_contributions` / `base_pot` / `pot_total` references).
   - Zero TBD placeholders in release notes (this is what the final substitution PR closes).
   - v1.5 Brown apples-to-apples test exists.
   - All inner-script preflight checks (clean tree, CI-green, version stamps, etc.).
   If dry-run is green → proceed to step 5.
5. **Fire v1.8.0 ship.** `bash scripts/release_v1_8_0_trigger.sh` from main. The wrapper dynamically reads origin/main HEAD SHA, stashes untracked docs, fires `scripts/release_v1_8_0.sh` (the inner one-shot ship script), tags + pushes + creates GitHub Release. Runbook: `docs/v1_8_0_ship_trigger_runbook_2026-05-27.md` (9-phase step-by-step, abort + recovery, idempotency boundaries).
6. **Build `.dmg` locally** per `docs/dmg_build_runbook_2026-05-26.md`. Decide on re-upload to the GitHub Release (Task #16).
7. **PR #20 Tier 2 user decisions.** 3 pre-existing test bugs identified during PR #20 CI matrix work need user disposition. Cross-platform CI matrix is otherwise rebased + @slow-marker-covered (PRs #82 + `b53e0f0`).
8. **Tasks #31, #32 (preflop orchestrator + RvR).** Multi-day scope decisions — defer until after v1.8.0 ship; these are forward-looking.

**Lower-priority (optional, anytime):**

- Decide on **PR #93** (terminal-utility ablation feature-flag). Now obsolete post-purge — likely close. If kept, would serve as backwards-compat regression-toggle.
- Check **perpetual QA iter 2** (`ae70403`). Iter 1 was 8/8 PASS per `docs/perpetual_qa_findings_2026-05-27.md`; iter 2 picks up P0↔P1 swap, Kuhn terminal utility sum-to-zero, Leduc cross-tier diff, range-parser round-trip, equity tie-handling.
- Clean **`pr-75-canonical-utility` worktree** — purge merged; safe to remove.
- Clean **`pr-79-fix-self-ref`, `pr-80-slow-markers` worktrees** — both merged.
- Reap **W4.2 persona retest agent `a545b89`** — > 26 hr old, likely stalled per [agent-execution-timeout](feedback_agent_execution_timeout.md). Spawn fresh if W4.2 retest still needed.
- Diff the **pruned PLAN.md** (branch `pr-89-plan-prune`, commit `a012de6`). Live PLAN.md was synced 102 KB → 48.9 KB in place (gitignored, not committed).
- Re-sync **backup/main** (currently at `5ead08f`, 38+ commits behind).
- Decide on **5 backup-branch deletes + 5 surface-branches** per `docs/backup_branch_audit_2026-05-26.md`.

---

## Audit-trail integrity (post-update)

One-line provenance per PR (second + third + fourth + fifth burst) — spot-checkable via `git show <SHA> --stat`:

**Fifth burst (PRs #77-#84) — KEYSTONE BURST:**
- `a44c93e` — `docs: v1.6.1 hold-lift VALIDATED post-purge (W4.3 strict-PASS confirms)` (#84) — `docs/v1_6_1_ship_hold_review_2026-05-26.md` banner flip.
- `839645e` — `docs: persona W4.3 reclassified PASS-strict post-convention-purge` (#83) — persona status doc edit + tracked `docs/persona_post_purge_retest_2026-05-27.md`.
- `f1e9c81` — `test: add @pytest.mark.slow to 2 more heavy tests in test_river_diff_self_sanity` (#82) — `tests/test_river_diff_self_sanity.py`.
- `9846827` — `docs: substitute PR #78 + SHA 37e5be1 in v1.8.0 release notes` (#81) — `docs/v1_8_0_release_notes_DRAFT.md`.
- `a824556` — `feat(release): one-shot v1.8.0 ship trigger wrapper` (#80) — `scripts/release_v1_8_0_trigger.sh` + `docs/v1_8_0_ship_trigger_runbook_2026-05-27.md`.
- `d1fcc3f` — `docs: fix self-ref in retracted-doc supersede banner (PR #75 -> PR #78)` (#79) — `docs/a83_deep_cap_root_cause_investigation.md`.
- **`37e5be1`** — **`fix(engine): purge 'rust' terminal-utility convention; canonical Brown formula is the only utility`** (#78) — **KEYSTONE PR.** 8 files / +214/-85: `crates/cfr_core/src/hunl.rs`, `crates/cfr_core/src/exploit.rs`, `poker_solver/hunl.py`, 5 test files rebaselined.
- `d5f17b3` — `docs: pre-fill v1.8.0 release notes for post-purge ship (TBD placeholders flagged)` (#77) — `docs/v1_8_0_release_notes_DRAFT.md`.

**Fourth burst (PRs #70-#76):**
- `9bd071f` — `docs: archive shipped wave logs + R1-R11 narrative from PLAN.md` (#76) — `docs/_archive_plan_2026-05-27/`.
- `44a9ee5` — `docs: supersede banners on retracted A83/terminal-utility docs (PR #75 purges 'rust' convention)` (#75).
- `3b9bd07` — `🚨 docs: A83 terminal-utility ablation contradicts arbitrator — needs reconcile` (#74) — `docs/A83_RECONCILE_URGENT_2026-05-26.md`.
- `eaca561` — `docs: README + USAGE final consistency polish (6 findings)` (#73).
- `0f290a8` — `docs: final integrity sweep — surface 3 morning-handoff artifacts` (#72).
- `c76aff8` — `docs: final consistency fix-up on v1.8.0 release notes + CHANGELOG` (#71).
- `fa83413` — `chore(gitignore): ignore transient session artifacts (release-script unblock)` (#70).

**Third burst (PRs #61-#69):**
- `b401f6c` — `feat: A83 Nash multiplicity empirically CONFIRMED via corrected probe` (#68) — `scripts/a83_nash_multiplicity_probe.py` + `docs/a83_nash_multiplicity_confirmed_2026-05-26.md`.
- `98fb503` — `fix(solver): hard-fail scalar HUNL postflop without initial_hole_cards` (#69) — `hunl_solver.rs` + `cli.py` + 2 test files.
- `d0b7b34` — `docs: surface pyenv x86_64 vs arm64 .so arch hazard for devs` (#67).
- `c15b952` — `docs: persona W3.3 PARTIAL->PASS` (#66).
- `c46621c` — `docs: surface Gate 4 + Track A invalidation; propose correct A83 probe` (#65).
- `8fa5b5b` — `docs(readme): mark ad-hoc postflop CLI as multi-minute; keep tiny_subgame as Quick-start` (#64).
- `533cb8e` — `docs: supersede banner on a83 RC investigation (math error in §2(d))` (#63).
- `1e4e60c` — `docs: v1.8.0 release notes final polish` (#62).
- `55122c3` — `docs: fix 2 broken reference paths` (#61).

**Second burst (PRs #50-#60):**
- `eb74fb3` — `docs: track previously-orphan reference targets ...` (#60) — 8 docs verbatim-tracked.
- `6d99a96` — `docs: persona status update post-W3.2/W3.4 retests` (#59) — `docs/persona_status_post_v1_8_2026-05-26.md`.
- `9948eaa` — `docs: add CHANGELOG note for poker-solver shim quirk` (#58) — `CHANGELOG.md`.
- `0d1c717` — `docs: resolve 7 MEDIUM stale claims` (#57) — `docs/medium_staleness_fix_pr_99_2026-05-26.md`.
- `bf645ae` — `docs: v1.8 release notes honesty (~1.0x not 4-8x) + W3.2 BR smoke` (#56) — `docs/v1_8_0_release_notes_DRAFT.md` + `docs/persona_w3_2_smoke_2026-05-26.md`.
- `7bb21d8` — `docs: resolve 8 HIGH stale claims` (#55) — `docs/doc_staleness_sweep_2026-05-26.md`.
- `97886e1` — `feat: v1.8.0 release execution script + .dmg build runbook` (#54) — `scripts/release_v1_8_0.sh` + `docs/dmg_build_runbook_2026-05-26.md`.
- `f165eb8` — `docs: archive 34 unreferenced 2026-05-23/25 session drafts` (#52) — `docs/_archive_2026-05-26_session/`.
- `29d608e` — `feat: --regret-init-noise flag` (#53) — CLI + Rust wiring.
- `3eea3b1` — `docs: persist terminal-utility audit + arbitration` (#51) — 3 audit/arbitration docs.
- `a5be2be` — `docs: persist A83 validation + update v1.8.0 release notes + lift v1.6.1 hold framing` (#50).

Every in-flight item is described honestly with disk state. Major Finding #1 is FULLY RESOLVED via the convention purge (PR #78) + Nash multiplicity residual + 4-layer gate PASS. Major Finding #2 (v1.8 SIMD ~1.0×) was settled via PR #56. Only the final TBD-substitution PR is between main and v1.8.0 ship.

---

## Source-of-truth pointers

- This document: `/Users/ashen/Desktop/poker_solver/docs/RESUME_2026-05-26.md` (post-merge of PR #49)
- **Convention purge KEYSTONE (PR #78, `37e5be1`):** code at `crates/cfr_core/src/hunl.rs`, `crates/cfr_core/src/exploit.rs`, `poker_solver/hunl.py`. Test rebaseline: `crates/cfr_core/tests/hunl_state_unit.rs`, `crates/cfr_core/tests/test_hunl_rust.rs`, `tests/test_hunl_core.py`, `tests/test_hunl_tree.py`, `tests/test_asymmetric_contributions.py`.
- **Post-purge v1.5 Brown apples-to-apples numbers:** `/Users/ashen/Desktop/poker_solver/docs/v1_5_brown_post_purge_numbers_2026-05-27.md` (PARTIAL verdict: PASS gated SANITY, residual is multiplicity floor not convention bug).
- **Post-purge persona retest sweep:** `/Users/ashen/Desktop/poker_solver/docs/persona_post_purge_retest_2026-05-27.md` (W4.3 strict-PASS upgrade source).
- **EV invariance sanity gauntlet design:** `/Users/ashen/Desktop/poker_solver/docs/ev_invariance_sanity_gauntlet_design_2026-05-27.md` (load-bearing testable invariant for Nash multiplicity residual).
- **v1.8.0 ship trigger wrapper (PR #80):** `/Users/ashen/Desktop/poker_solver/scripts/release_v1_8_0_trigger.sh` + runbook at `/Users/ashen/Desktop/poker_solver/docs/v1_8_0_ship_trigger_runbook_2026-05-27.md`.
- **A83 Nash multiplicity confirmation (PR #68):** `/Users/ashen/Desktop/poker_solver/docs/a83_nash_multiplicity_confirmed_2026-05-26.md`; probe script `/Users/ashen/Desktop/poker_solver/scripts/a83_nash_multiplicity_probe.py`.
- **Ship-blocker RCA + repair (PR #69):** `/Users/ashen/Desktop/poker_solver/docs/chance_outcomes_empty_rca_2026-05-26.md`.
- **Terminal-utility arbitration (RETRACTED via PR #78):** `/Users/ashen/Desktop/poker_solver/docs/terminal_utility_arbitration_2026-05-26.md` — kept for audit; verdict superseded by empirical purge.
- **v1.6.1 ship-or-hold review (HOLD LIFTED, VALIDATED via PR #84):** `/Users/ashen/Desktop/poker_solver/docs/v1_6_1_ship_hold_review_2026-05-26.md`.
- **v1.8.0 release-notes draft (on main; 2 TBDs remaining for final substitution PR):** `/Users/ashen/Desktop/poker_solver/docs/v1_8_0_release_notes_DRAFT.md`.
- **v1.8.0 inner ship script (PR #54):** `/Users/ashen/Desktop/poker_solver/scripts/release_v1_8_0.sh`.
- **`.dmg` build runbook:** `/Users/ashen/Desktop/poker_solver/docs/dmg_build_runbook_2026-05-26.md`.
- **A83 3rd validator audit:** `/Users/ashen/Desktop/poker_solver/docs/a83_validation_2026-05-26.md`.
- **A83 Track A setup:** `/Users/ashen/Desktop/poker_solver/docs/a83_track_a_setup_2026-05-26.md`.
- **A83 deep-cap RC investigation (superseded; banner via PR #63 + #79):** `/Users/ashen/Desktop/poker_solver/docs/a83_deep_cap_root_cause_investigation.md`.
- **A83 reconcile urgent doc (made moot by PR #78):** `/Users/ashen/Desktop/poker_solver/docs/A83_RECONCILE_URGENT_2026-05-26.md`.
- **Persona status post v1.8:** `/Users/ashen/Desktop/poker_solver/docs/persona_status_post_v1_8_2026-05-26.md`.
- **Perpetual QA findings (iter 1 PASS; iter 2 in flight):** `/Users/ashen/Desktop/poker_solver/docs/perpetual_qa_findings_2026-05-27.md`.
- **PLAN.md prune report:** `/Users/ashen/Desktop/poker_solver/docs/plan_prune_pr_89_2026-05-26.md`.
- **Backup-branch audit:** `/Users/ashen/Desktop/poker_solver/docs/backup_branch_audit_2026-05-26.md`.
- **pyenv `.so` arch hazard doc (PR #67):** `/Users/ashen/Desktop/poker_solver/docs/pyenv_so_arch_hazard_2026-05-26.md`.
- Gate 4 200K v2 logs (INVALIDATED; preserved for forensics): `/tmp/gate4_200k_v2_{river,turn,setup,nohup}.log` + `/tmp/gate4_200k_v2_done.flag`. Future invocations now hard-fail via PR #69.
- A83 correct-entrypoint probe artifacts (NOT in repo): `~/Desktop/a83_correct_probe_{baseline,perturbed,summary}.json` + `.log`.

---

## Session metrics — end-of-session totals

Source: `docs/session_metrics_2026-05-26.md` (already on main). Numbers below evolve across passes.

- **PRs merged on `origin/main` (pre-third-burst):** 25 (16 docs-only, 9 code-changing).
- **PRs merged on `origin/main` (post-third-burst, fourth-pass update):** 34 (24 docs-only, 10 code-changing — adds PR #69 ship-blocker + PR #68 multiplicity-probe code).
- **PRs merged on `origin/main` (post-fourth-burst, seventh-pass update):** 41 (31 docs-only, 10 code-changing — adds PRs #70-#76, all docs/chore).
- **PRs merged on `origin/main` (post-fifth-burst, THIS UPDATE):** **49** (37 docs-only, 12 code-changing — adds PRs #77-#84; **PR #78 is the convention-purge KEYSTONE**, PR #80 is the ship-trigger wrapper, PR #82 is a test marker).
- **PRs closed without merge:** 1 (#34, superseded by #62).
- **Cumulative diff vs session start:** significantly higher than the 112-file / +16,818/-444 figure in the session metrics doc — re-run accounting deferred to next session.
- **Memory entries edited:** 6 of 39 (`feedback_*.md` updates).

End-of-session verification matrix: `/Users/ashen/Desktop/poker_solver/docs/end_of_session_verification_2026-05-26.md`.

---

**Compiled by:** RESUME pre-draft agent (2026-05-26, ~30-min budget; one-shot synthesis from 21 session artifacts + git log + open-PR list + worktree state). **Updated** in second pass (~07:45 UTC 2026-05-26) to fold in PRs #50-#60. **Third pass** (~07:55 UTC 2026-05-26): end-of-session metrics. **Fourth pass** (~04:30 UTC 2026-05-26, post-probe): folded in A83 Nash multiplicity empirical confirmation (PR #68) + ship-blocker hot-patch (PR #69) + third-burst PRs #61-#67, persona counts. **Fifth pass** (~09:20 UTC 2026-05-26): downgraded VICTORY → IN FLUX after PR-93 ablation. **Sixth pass** (~03:57 UTC 2026-05-27): VICTORY banner replaced after convention-purge work began. **Seventh pass** (~08:00 UTC 2026-05-27): mid-AFK refresh — fold in fourth-burst PRs #70-#76, convention-purge recovery story (silent agent death + recovery agent in flight). **Eighth pass (THIS UPDATE, ~09:30 UTC 2026-05-27)**: convention purge LANDED as PR #78 (KEYSTONE); fold in fifth-burst PRs #77-#84; A83 status FULLY RESOLVED via nuanced two-mechanism story (convention purge closed regret-update bias + Nash multiplicity residual handled by EV invariance gauntlet); W4.3 PASS-aggregator → PASS-strict; v1.6.1 hold-lift VALIDATED; v1.8.0 ship trigger wrapper VERIFIED via dry-run; ship ready to fire on final TBD-substitution PR landing.
**This PR (#49):** HOLD. User reviews + merges in the morning. No auto-merge.
